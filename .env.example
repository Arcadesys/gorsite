# --- NextAuth ---
NEXTAUTH_URL=
NEXTAUTH_SECRET=

# --- Supabase (client) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPERADMIN_EMAIL=
SETUP_TOKEN=
NEXT_PUBLIC_SUPABASE_BUCKET=artworks
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# --- Supabase (database / Prisma) ---
# Use the Connection Pooling (pgBouncer) string for DATABASE_URL
# Example format (replace placeholders):
# DATABASE_URL="postgresql://postgres:<PASSWORD>@db.<PROJECT>.supabase.co:5432/postgres?pgbouncer=true&connection_limit=1"
DATABASE_URL=

# Use the Direct connection string for DIRECT_URL (migrations)
# Example format (replace placeholders):
# DIRECT_URL="postgresql://postgres:<PASSWORD>@db.<PROJECT>.supabase.co:5432/postgres"
DIRECT_URL=
